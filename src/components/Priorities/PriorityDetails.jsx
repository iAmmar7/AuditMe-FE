import React from 'react';
import { Card } from 'antd';

function PriorityDetails({ item }) {
  console.log(item);
  return <Card>Issue detail will be displayed here</Card>;
}

export default PriorityDetails;
